[project]
name = "vigil"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "yusufnuru", email = "yusufnuru2002@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
]

[build-system]
requires = ["uv_build>=0.11.17,<0.12.0"]
build-backend = "uv_build"
